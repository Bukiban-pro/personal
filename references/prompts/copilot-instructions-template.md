# AGENTS.md — [PROJECT_NAME]

```
This file is my brain. I improve it.
When I make a mistake, I update this so I never repeat it.
The goal: become incapable of repeating old mistakes.
```

## Project Purpose
What this system does, who it serves, what matters most.

## Architecture
```
[2-3 level repo map]
```

## Commands
- Test: `[command]`
- Build: `[command]`

## Operating System

**THINKING:**
- Understand before acting. Gather context. Identify failure modes. Then code.
- One thing at a time. Complete. Verify. Then advance.
- Every mistake is a thinking failure. Inscribe the lesson here.
- Hierarchy: BEST (ideal UX) > VISION (specs) > IMPLEMENTATION (code).

**EXECUTION:**
- 3-stage gate: AUDIT spec → VISION fix spec → IMPLEMENT code
- Feature lifecycle check: beginning + middle + end. No half-built features.
- Anti-pattern: shotgunning → STOP and diagnose. Assumption creep → VERIFY code.

**POST-SESSION:** VERIFY + REFLECT + EXTRACT + INSCRIBE.

## Project Knowledge

<!-- FILL IN: -->
- Architecture (components, tech, ports)
- Key files (configs, routes, types)
- Patterns (naming, structure, API style)
- Gotchas (append as found)

## Rules
- Prefer small diffs. Reuse patterns. Read tests near changed code.
- Backend = source of truth for types.
- Full-stack: fix backend FIRST, then frontend.
- UX: loading states, confirmations, keyboard shortcuts. Non-negotiable.
