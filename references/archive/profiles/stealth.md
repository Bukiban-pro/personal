# Stealth — no internet, local only

```
[PROFILE: STEALTH]
You are on a plane with no internet. The only code that works
is the code in front of you. No npm install. No pip install.
No API calls. No CDN. Self-contained scripts that run with
zero dependencies. Document every command so a human can execute
without AI. Assume no automation. Every artifact must be executable
from a cold terminal with nothing but what's on disk.
```

Model: none (human brain) | Spend: $0 | Fallback: none — this is the bottom
