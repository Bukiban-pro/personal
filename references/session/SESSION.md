# Session: 2026-07-17

**Mission**: Harden `personal` as a universal AI workflow OS without erasing its archive value.
**Formula**: Hot path = `prep scan -> SCOPE from safe scans -> prep grid -> SHOT in repo`; cold memory = archive/research mined only when useful.
**Energy**: 100%
**State**: verified pending fresh-agent test.

## Recovery Addendum: WEAPONS FACTORY Audit

Commit `b4af65c` correctly pushed `personal` toward a faster hot-path OS, but it overcompressed the doctrine: "not needed to boot" became "not valuable." That was wrong.

Recovered archive/research/prompt material is now cold memory, not active clutter. Use it for prompt DNA, older OS learning, UI/research source material, and historical strategy. Verify stale facts before use. Delete only after extraction, duplication proof, secret risk, or a ledger entry.

## What Was Built

| Artifact | What | Status |
|----------|------|--------|
| `BELT.md` | Promoted `prep` as canonical dispatcher, aligned role order and scenario flows, and added archive doctrine. | done |
| `AGENTS.md` | Reframed BELT as hot-path boot OS and protected archive/research as cold memory. | done |
| `hands/prep.ps1` | Defaults to current repo, passes `-RepoPath`, `-Minimal`, and `-NoTabs` through correctly. | done |
| `hands/context-pack.ps1` | Accepts `-Mission`, writes optional output files, makes `ultra` structural instead of raw short-line code, redacts table names/secrets, and handles clipboard failure honestly. | done |
| `hands/boot-session.ps1` | Aligns Tab 1 SHOT / Tab 2 SCOPE with checklists, supports current-repo session loading, and avoids false clipboard success. | done |
| `hands/run-card.ps1` | Parses CRLF one-line prep cards, executes `prep scan` / `prep grid`, and supports `-NoTabs`. | done |
| Scenario cards + formula backup | Removed stale `recon` and Tab1=SCOPE assumptions from active cards; corrected archive path references. | done |
| `references/archive/RECOVERY_INDEX.md` | Classifies restored material as living doctrine seed, reusable artifact, historical/stale reference, or deletion candidate. | done |

## Verification

- `prep list`
- `context-pack.ps1 -Pack ultra -Anonymize -NoClipboard` on `personal/payment-api/src/main/java`
- `prep outside -Mission "Smoke outside" -RepoPath personal/payment-api/src/main/java`
- `prep grid -Mission "Smoke grid" -RepoPath personal -NoTabs`
- `prep card 01-unknown-repo -RepoPath personal/payment-api -Mission "Smoke card" -NoTabs`
- `git diff --diff-filter=D b4af65c^ b4af65c` to reconstruct the deleted cluster
- Doctrine sweep for purge language in BELT, AGENTS, SCRATCHPAD, SESSION, and formula backup

## Corrected Preference

- `[PREF: every file must earn its existence]` means hot-path files need immediate execution value; archive/research files need recoverable prompt, pattern, story, source, or historical value.
- `[PREF: deletion requires extraction or ledger]` If a file's unique value cannot be named and replaced, keep it as cold memory.
- `[PREF: scenario cards beat matrix tables under pressure]` Drop-in prompts remain the fastest operational interface.
- `[PREF: hard constraints beat suggestions]` Corp-sec rules stay FORBIDDEN/ALLOWED, not "consider this."

## Next Actions

1. Test BELT transformation in a fresh web/IDE agent.
2. Run the unknown-repo 15-minute checklist on a real repo.
3. Periodically mine `references/archive/RECOVERY_INDEX.md` for patterns worth promoting into hot-path docs.

---

# Session: 2026-07-16

**Mission**: Transform personal from a loose stash into a prep-time operating system.
**State**: shipped, then corrected by the 2026-07-17 recovery pass.

## Historical Note

The hot-path compression was useful: BELT, SCOPE/SHOT ignition, scenario cards, wrappers, and DROPZONE made the system faster. The purge conclusion was too shallow: archive, prompt, and research material had unique value even when it was not required for boot.

## Durable Lesson

Do not confuse "not primary" with "not valuable." Archive material can be doctrine seed, prompt DNA, research source, or historical proof of how the OS learned.
